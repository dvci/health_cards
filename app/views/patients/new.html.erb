<section class="hero is-small is-info">
  <div class="hero-body">
    <p class="title">New Patient</p>
  </div>
</section>

<%= button_to 'Demo Data', new_patient_path, params: fake_patient_params, method: :get, class: 'button is-info is-light' %>
<%= render 'form', patient: @patient %>
