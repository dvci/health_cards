KEY_PATH=config/keys/dev_key.pem
# User should pass in their own HOST environment variable when running in production, to be used as the iss value in the SMART Health Card JWS.
HOST=http://localhost:3000
AUTH_CODE=test_code
CLIENT_ID=test_client 